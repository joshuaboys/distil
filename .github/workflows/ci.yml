name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm build

      - run: pnpm typecheck

<<<<<<< HEAD
      - run: pnpm coverage
||||||| parent of 55b45a7 (ci: run coverage only on node 22, test on node 20)
      - run: pnpm test
=======
      - run: pnpm test
        if: matrix.node-version != 22

      - run: pnpm coverage
        if: matrix.node-version == 22
>>>>>>> 55b45a7 (ci: run coverage only on node 22, test on node 20)

      - name: Upload coverage
        if: matrix.node-version == 22
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: packages/*/coverage/
          retention-days: 14
